const ORIGIN = "https://192.168.1.71:8080";

export default {
  auth: {
    login: `${ORIGIN}/auth/login`,
    register: `${ORIGIN}/auth/register`,
  },
  resources: {
    app: `${ORIGIN}/app`,
  },
};
